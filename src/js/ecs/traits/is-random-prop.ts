import {trait} from "koota";

// we only use this trait for demo purposes, it's
// used to spawn a random body in the example actions store.
// it doesn't relate to jolt in any way.
export const IsRandomProp = trait();